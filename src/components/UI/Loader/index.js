import React from 'react';
import './Loader.scss'

const Loader = (props) => (
    <div className="LoaderWrapper">
        <div className="Loader">
            <div/>
            <div/>
        </div>
    </div>
);

export default Loader;